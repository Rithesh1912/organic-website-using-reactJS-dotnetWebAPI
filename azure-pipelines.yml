trigger:
- organicfrontend

pool:
  vmImage: ubuntu-latest

steps:

- checkout: self

- script: |
    echo "===== CURRENT DIRECTORY ====="
    pwd
    echo "===== ROOT FILES ====="
    ls
    echo "===== FULL STRUCTURE ====="
    ls -R
  displayName: 'DEBUG - Show folder structure'